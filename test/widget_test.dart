import 'package:flutter_test/flutter_test.dart';

/// Placeholder widget test. Real tests added in M2.
void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}

import 'package:flutter/material.dart';

/// Step 4: Add caption, photos, sensory tags before publishing the Stamp.
class RecordEditScreen extends StatelessWidget {
  const RecordEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Details')),
      body: const Center(child: Text('Record Edit — TODO(M2)')),
    );
  }
}
